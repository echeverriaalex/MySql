-- la cantidad de columnas deben ser la misma para ambos selecets 4 y 4 y asi
select e.id, e.nombre, d.id, d.nombre
	from 
    where
    union all
select e.id, e.nombre, null, null 

	order by 2, 3  -- por numero de columna la columna 2 y despues la 3